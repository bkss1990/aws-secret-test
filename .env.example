# Server Configuration
PORT=3000
NODE_ENV=development

# AWS Configuration
# AWS_REGION is optional - will use instance metadata if not set
AWS_REGION=us-east-1

# CORS Configuration
CORS_ORIGIN=*

# Optional: Secret name to read on startup
SECRET_NAME=my-secret-name
